# 页面用例规约（Page UCS Template）

> 每个页面一份，按此结构逐节填写。组件名以本页组件切片（`docs/specs/design/.slice/<页面>.md`，自 `COMPONENTS.md` 派生）的规范组件为准；无切片时回退所选组件库（`{组件库}` 占位，如 Element Plus / Ant Design / TDesign / uni-ui 等）。

# {页面名}（{页面文件}）

## URL

{路由路径，如 /trip-collection}

## 数据源

- {接口1}：{HTTP方法} {路径}（{加载条件，如"页面加载时"、"仅当某状态时"、"懒加载"})
- {接口2}：{HTTP方法} {路径}（仅当 {条件} 时加载）
- 本地计算：{说明，如倒计时基于某时间字段计算}

> 接口名/路径以 docs/specs/API 为准。

## 组件树

- {领域组件1}（{职责一句话}）
- {领域组件2}
- {通用组件（规范组件名，如 Button(primary) / Card / Modal）}：
  - 若 {条件}：显示 "{规范组件名}"（{行为，如"需二次确认弹窗"、"倒计时"、"只读"})
  - 若 {条件}：显示 "{规范组件名}"
  - 若 {条件}：禁用 / 隐藏

## 组件调整

基于规范组件（映射到目标端实际组件），本页需要做哪些样式/行为调整（尺寸、间距、主题色、状态色等）；颜色/字号/间距用 `docs/specs/design/DESIGN.md` 的 token 引用，不写具体值。

## 交互流

1. **页面加载**
   - 显示骨架屏
   - 并行请求：{接口1} +（{接口2}，带条件跳过）
   - 加载失败：{重试 / 空态提示}
2. **用户点击"{操作}"**
   - 弹出 {Modal/确认框} 确认
   - 调用 {HTTP方法} {路径}
   - 成功：Toast 提示，刷新页面状态
   - 失败 {错误码}：Toast "{提示文案}"
3. **{边界场景}（如倒计时归零 / 重复提交 / 网络中断）**
   - 自动刷新 / 回退 / 禁用操作按钮

## 写作原则

- 组件名以本页组件切片（docs/specs/design/.slice/<页面>.md）的规范组件为准，不写库外组件名；无切片时以所选组件库（{组件库}）为准
- 数据源接口名/路径以 docs/specs/API 为准，字段对齐接口
- 交互流覆盖：加载（骨架屏/并行/条件）、主操作（确认/调用/成功/失败）、边界场景
- 组件调整只写"相对规范组件"的差异，不重复组件切片内容
