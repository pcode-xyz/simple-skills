# CLAUDE.md

## 1. 先思考，后编码

**不要臆测，不要隐藏困惑，要暴露取舍。**

在实现之前：
- 明确指出你的假设。如有不确定，请提问。
- 若存在多种解读，请一并列出——不要悄悄选一个。
- 若有更简单的做法，请直言。如有必要，可以提出反对。
- 若有不明确之处，请停下来。指出困惑点，并提出疑问。

## 2. 简单至上

**编写解决问题的最少代码。不做推测性工作。**

- 不添加未要求的功能。
- 不为一次性代码创建抽象。
- 不添加未要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景编写错误处理。
- 如果你写了 200 行而实际只需 50 行，请重写。

## 3. 手术式改动

**只动你必须动的地方。只清理你自己造成的混乱。**

在修改现有代码时：
- 不要“改进”相邻的代码、注释或格式。
- 不要重构没有问题的部分。
- 遵循现有风格，即使你自己会采用不同做法。
- 如果你发现无关的死代码，可以提一下，但不要删除。

当你的改动产生“孤儿”时：
- 移除因你改动而不再使用的导入/变量/函数。
- 除非被要求，否则不要移除预先存在的死代码。

检验标准：每一处改动都应能直接追溯到用户的需求。

## 4. 检索范围

**除非明确指定，否则只检索当前项目目录。**

- 不要向父级目录或系统目录扩散搜索。
- 若需要检索项目外的内容（如全局配置、其他仓库），需用户明确指示。

## 5. 目标驱动执行

**定义成功标准。循环验证，直到满足。**

将任务转化为可验证的目标：
- “添加校验” → “为非法输入编写测试，然后让它们通过”
- “修复 Bug” → “编写能复现该 Bug 的测试，然后让测试通过”
- “重构 X” → “确保重构前后测试均通过”

对于多步任务，简述计划：
```
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
```

强有力的成功标准能让你独立进行循环迭代。弱标准（比如“把它弄好”）则需要不断澄清。
